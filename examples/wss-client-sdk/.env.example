# API Key for authentication with the WebSocket server
SOR_API_KEY=your_api_key_here

# Transaction signature to monitor (optional)
# If not provided, will use a default test signature
TEST_SIGNATURE=your_transaction_signature_here

# WebSocket server URL (optional)
# If not provided, will use the default staging URL
WS_SERVER_URL=wss://staging-sor-api-437363704888.asia-northeast1.run.app/v1/observe_tx 